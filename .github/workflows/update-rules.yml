name: Update Rules

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"
  push:
    paths:
      - rules/*/manifest.yaml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - run: |
          for client in mihomo sing-box surge; do
            bash .github/scripts/build.sh rules "$client"
          done

          source .github/scripts/build.sh

          sync_changes rules
